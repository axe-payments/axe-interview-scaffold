# Copy this file to `.env` and paste in the Vapi API key you were given at the
# start of the interview:  cp .env.example .env
#
# The API key is the only value you need. Everything else (the agent and
# phone-number IDs) is already wired up in the code.

VAPI_API_KEY=
